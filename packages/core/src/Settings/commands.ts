export function addMenuCommands(menu: HTMLElement) {
  menu.addEventListener('keydown', (event) => {
    
  })
}

export function addTreeCommands(tree: HTMLElement) {
  tree.addEventListener('keydown', (event) => {
    
  })
}